import { standings } from '@/data/points-table';
import { teams } from '@/data/teams';
import { SectionHeading } from './SectionHeading';
export function Standings() { return <section className="section-space bg-white"><div className="section-shell"><SectionHeading eyebrow="The race for the cup" title="Points table" action={{ label: 'Full standings', href: '/schedule' }}/><div className="overflow-hidden rounded-2xl border border-slate-200"><div className="grid grid-cols-[2fr_repeat(4,1fr)] bg-navy px-4 py-4 text-[10px] font-bold uppercase tracking-widest text-white/70 sm:px-6"><span>Team</span><span className="text-center">P</span><span className="text-center">W</span><span className="text-center">NRR</span><span className="text-center">Pts</span></div>{standings.map((row, index) => { const team = teams.find((item) => item.id === row.teamId)!; return <div key={row.teamId} className="grid grid-cols-[2fr_repeat(4,1fr)] items-center border-t border-slate-100 px-4 py-4 text-sm sm:px-6"><div className="flex items-center gap-3"><span className="display text-lg font-bold text-slate-300">{String(index + 1).padStart(2, '0')}</span><span className="team-mark h-9 w-9 rounded-lg text-[10px]" style={{ backgroundColor: team.primaryColor }}>{team.shortName}</span><span className="hidden font-bold sm:block">{team.name}</span><span className="font-bold sm:hidden">{team.shortName}</span></div><span className="text-center text-slate-500">{row.played}</span><span className="text-center text-slate-500">{row.won}</span><span className="text-center font-semibold text-brandGreen">{row.nrr}</span><span className="text-center font-bold text-navy">{row.points}</span></div> })}</div></div></section> }
