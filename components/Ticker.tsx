import { ticker } from '@/data/ticker';
export function Ticker() { const items = [...ticker, ...ticker]; return <div className="overflow-hidden bg-brandGreen text-white"><div className="section-shell flex h-11 items-center gap-4"><span className="flex shrink-0 items-center gap-2 text-xs font-bold uppercase tracking-[.18em]"><i className="pulse-dot h-2 w-2 rounded-full bg-white"/> Live feed</span><div className="overflow-hidden whitespace-nowrap"><div className="animate-marquee inline-flex min-w-max gap-12 text-xs font-semibold">{items.map((item, i) => <span key={`${item}-${i}`} className="after:ml-12 after:text-white/50 after:content-['✦']">{item}</span>)}</div></div></div></div> }
