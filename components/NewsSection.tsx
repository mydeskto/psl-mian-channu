import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { news } from '@/data/news';
import { SectionHeading } from './SectionHeading';
export function NewsSection() { return <section className="section-space bg-paper"><div className="section-shell"><SectionHeading eyebrow="From around the league" title="Latest news" action={{ label: 'All stories', href: '/news' }}/><div className="grid gap-5 lg:grid-cols-3">{news.map((item, index) => <article key={item.id} className={`group overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-card ${index === 0 ? 'lg:col-span-2 lg:grid lg:grid-cols-[1fr_1.2fr]' : ''}`}><div className="flex min-h-48 items-center justify-center bg-navy p-8"><img src={item.imageUrl} alt="" className="h-28 w-28 rounded-full object-cover opacity-80 transition group-hover:scale-105"/></div><div className="p-6"><p className="text-[10px] font-bold uppercase tracking-widest text-brandGreen">{item.category} · {item.date}</p><h3 className="display mt-2 text-3xl font-bold uppercase leading-none text-navy">{item.title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{item.summary}</p><Link href={`/news/${item.id}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-navy">Read story <ArrowUpRight size={15}/></Link></div></article>)}</div></div></section> }
