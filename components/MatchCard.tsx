import { CalendarDays, MapPin } from 'lucide-react';
import { teams } from '@/data/teams';
import type { Match } from '@/data/schedule';
const statusStyle = { upcoming: 'bg-slate-100 text-navy', live: 'bg-red-50 text-red-600', completed: 'bg-green-50 text-brandGreen' };
export function TeamBadge({ teamId, small = false }: { teamId: string; small?: boolean }) { const team = teams.find((item) => item.id === teamId)!; return <div className="flex items-center gap-2"><span className={`team-mark ${small ? 'h-8 w-8 rounded-lg text-[10px]' : 'h-12 w-12'} bg-[${team.primaryColor}]`} style={{ backgroundColor: team.primaryColor }}>{team.shortName}</span><span className="font-bold">{team.name}</span></div> }
export function MatchCard({ match }: { match: Match }) { const a = teams.find((item) => item.id === match.teamAId)!; const b = teams.find((item) => item.id === match.teamBId)!; return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-card transition hover:-translate-y-1"><div className="mb-5 flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-widest text-slate-400">Match {String(match.matchNumber).padStart(2, '0')}</span><span className={`rounded-full px-3 py-1 text-[10px] font-bold uppercase tracking-widest ${statusStyle[match.status]}`}>{match.status === 'live' ? '● Live now' : match.status}</span></div><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-3"><div className="text-center"><span className="team-mark mx-auto mb-2 h-12 w-12 text-xs" style={{ backgroundColor: a.primaryColor }}>{a.shortName}</span><p className="text-sm font-bold">{a.shortName}</p></div><span className="display text-2xl font-bold text-slate-300">VS</span><div className="text-center"><span className="team-mark mx-auto mb-2 h-12 w-12 text-xs" style={{ backgroundColor: b.primaryColor }}>{b.shortName}</span><p className="text-sm font-bold">{b.shortName}</p></div></div><div className="mt-5 space-y-2 border-t border-slate-100 pt-4 text-xs text-slate-500"><p className="flex items-center gap-2"><CalendarDays size={14} className="text-brandGreen"/>{match.date} · {match.time}</p><p className="flex items-center gap-2"><MapPin size={14} className="text-brandGreen"/>{match.venue}</p></div>{match.result && <p className="mt-4 rounded-lg bg-paper px-3 py-2 text-xs font-bold text-navy">{match.result}</p>}</article> }
